name: Build

on:
  push:
    branches:
      - main
      - "*/dev"
  pull_request:
    branches:
      - main

jobs:
  typecheck:
    name: TypeScript check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: less-gram/package.json

      - name: Install dependencies
        working-directory: less-gram
        run: npm install

      - name: Type check
        working-directory: less-gram
        run: npx tsc --noEmit
